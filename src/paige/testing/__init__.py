"""Test-only fakes implementing paige's ports.

By design, NOTHING in `paige.domain` / `paige.ports` /
`paige.application` / `paige.adapters` may import this package —
import-linter enforces. Tests can import freely.
"""
